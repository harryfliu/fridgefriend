import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
};

export const metadata: Metadata = {
  title: "Fridge Friend - Turn Your Ingredients into Recipes",
  description: "AI-powered recipe generator that creates delicious meals using only the ingredients you have. Simple, fast, and free!",
  keywords: ["recipe generator", "cooking", "ingredients", "AI recipes", "meal planning"],
  authors: [{ name: "Harry Liu" }],
  openGraph: {
    title: "Fridge Friend",
    description: "Turn your ingredients into delicious recipes",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
